<h1>Editing Post</h1>

<%= render 'form', post: (@bulletin.present? ? @post.bulletin : @post) %>

<%= link_to 'Show', @bulletin.present? ? bulletin_post_path : post_path %> |
<%= link_to 'Back', @bulletin.present? ? bulletin_posts_path : posts_path %>